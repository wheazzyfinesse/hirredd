/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [{
            hostname: "zy99ovvfgn07dhko.public.blob.vercel-storage.com"
        }]
    }
};

export default nextConfig;
